#include "MenuManager.h"

void MenuManager::Draw() const
{
	switch (state)
	{
	case Main:
		// Draw & call actions that are needed
		break;
	case LevelSelect:
		// Draw & call actions that are needed
		break;
	}
}
